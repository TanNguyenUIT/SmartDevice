import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  main: {
    flex: 1
  }
});

const AddDevice = () => (
  <View style={styles.main}>
    <Text>this is add device</Text>
  </View>
);

export default AddDevice;
